{
  "buildCommand": "pnpm --filter @chat-app/frontend build",
  "outputDirectory": "packages/frontend/dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
